# 설계 결정 기록

공개 이전 개발 과정에서 내려진 주요 결정과 그 근거.
커밋 단위 이력이 아니라, **왜 지금 모양이 되었는지**의 기록이다.
되돌린 결정도 남긴다 — 같은 길을 두 번 가지 않기 위해.

---

## 옵션 모델의 원칙

**법이 정하면 상수, 설계자가 고르면 옵션.**
주차 칸 치수(2.5×5.0m)나 채광면적 하한 같은 법정 수치는 `BuildOptions`에
노출하지 않는다. 옵션으로 두면 "법을 어길 수 있는 입력"이 생긴다.

**옵션은 의도(intent) 레이어다.** 치수가 아니라 의도를 받는다.
그래서 여러 필드가 도입되었다가 제거됐다:

- `Core.width`/`depth` 제거 — 코어 치수는 입력이 아니라 derive 대상
- `Core.position`·`lateral` 제거 — 위치도 같은 이유
- `Pillars`(기둥 개수) → `Structure`(벽식/라멘/철골) — 개수는 결과, 구조방식이 의도
- `SpatialLayout` 도입 후 철회 — 옵션에 기하를 넣으면 의도 레이어가 무너진다

**목적함수(`objective`)는 폐기됐다.** "좋음"의 우선순위를 사용자가 리스트로
주는 방식(`attach` 추가 → 제거 → 전체 폐기)을 세 번 시도한 끝에,
선택은 엔진 내부 결합 목적함수로 완결하는 것으로 정리했다.
사용자가 가중치를 튜닝하게 만드는 설계는 실패했다.

---

## 창 — 랜덤 배치에서 법정 역산으로

창은 원래 WWR 비율 + `pattern`·`alignment`·`seed`로 배치했다.
난수로 창을 뿌리는 방식이었고, 결과가 재현되지 않았다.

**피난방화규칙 17조① — 거실 바닥면적의 1/10 이상 채광**을 기준으로
역산하는 방식으로 바꾸면서 세 필드를 전부 삭제했다.
남은 건 `size_ratio` 하나뿐이다: 법정 최소 채광면적의 몇 배로 잡을지.

법이 답을 정해주는 곳에서는 난수를 쓸 이유가 없다.

---

## 1층 — 절차로 푸는 문제

1층(필로티·주차·보행로·코어 진입)은 최적화가 아니라 **순차 절차**로 푼다.
`GroundFloor`의 필드들은 그 절차의 각 단계에 대응한다.

주요 결정:

- `core_edge`/`align`/`offset` 3필드 → `core_side` 8방위 단일 필드로 통합.
  중앙 의도를 표현할 수 없어 `"c"`를 추가했다.
- `pedestrian_width` 기본값을 **시행령 41조 다세대 유효너비 하한**에 맞춰
  1.5m로 정렬. 그 전엔 내부 관행값 1.2를 쓰고 있었다.
- `interior_aisle`은 3태다 — `None`=법정 대수 미달 시 자동 평가,
  `True`=우선 사용(강제 아님, 법정 충족 시 채택), `False`=금지.
  `True`를 "강제"로 해석하면 대수가 오히려 줄어드는 회귀가 있었다.
- `road_setback` — 주차 전면선 도로셋백. `None`이면 주차장법 시행규칙
  11조⑤2호의 도로 산입 규칙으로 derive.
- `core_entries`(2026-08, #35) — 코어 보행 출입구 2개. 계약은 `_pedestrian_paths`(복수,
  [0]=기존 `_pedestrian_path` 호환). `_halls`(2026-08, #30 D4) — 편복도(공용) 층별 ring:
  세대가 코어 복도에 직접 못 닿을 때 1.5m 띠를 뻗는다. 복도=유도값 규약은 유지하되
  채점기가 생성기와 같은 연결계를 쓰도록 방출.
- `parking_angle`(2026-08) — 내부차로 사선 주차 45°/60°. 외주 편집기
  v0.5.1의 각도 주차를 받되 차로폭은 원전(6m 고정)이 아니라 **법정값**
  (11조⑤1호: 45° 3.5m·60° 4.0m)으로 — 6m 고정이면 사선이 직각을 이길 수
  없어 버튼용 장식이 된다. 등맞대기 띠 폭 16→14.1m가 사선의 실질 가치.
  외부 도로변은 항상 직각(11조⑤2호). `None`=자동(미달 시 90→60→45).

---

## 주차 — 법정 대수와 8대 특례

주차 칸 치수·8대 특례(주차장법 11조⑤)·그룹 단위는 엔진 상수다.
옵션으로 노출된 건 "설계자가 고를 수 있는 것"뿐이다:
연접 백칸 허용, 내부 차로 모드, 다중 도로 진입 전략.

8대 특례 밖의 일반 부설주차장은 총 8대 캡이 적용되지 않는다 —
이 경계를 옵션이 아니라 엔진이 판정하도록 두었다.

---

## 콘크리트 — LOD 300 부재 치수

슬래브·기둥·벽 두께를 현장 실측과 참조 도면 기준으로 정렬했다.
초기값은 교과서 수치였고, 실제 시공 도면과 맞지 않았다.

- 슬래브 0.20 → 0.15, 기둥 0.40 → 0.60
- 세대간 경계벽: 법정 최소 0.15, 실무 기준 0.20 (지하 옹벽 0.30)
- 파라펫: 저벽 높이와 난간을 분리. 기본값 0 — 난간은 동자 + 손스침만.
- 난간 간살 굵기 50mm, **안목 120mm**.
  법정 기준은 간살 중심 간격이 아니라 **안목(순간격)**이다. 이 구분을
  틀리면 법정 미달인데 통과한 것으로 계산된다.

기초·단열·마감·전이보·계단 치수 필드를 LOD 300 수준으로 추가.

---

## 타입 계약 — 단일 진실

`scheme.json`·`units.json`의 형태는 생성기가 방출하는 실제 계약이다.
Python `types.py`와 TypeScript `types.ts`가 같은 것을 다르게 정의하는
드리프트가 반복되어, 규율을 세웠다:

> 생성기가 새 키나 `userData` kind를 방출하려면 **먼저** 양쪽 타입에
> 정의하고 sbim을 푸시한 뒤, 소비처가 import한다.

이 규율을 어긴 필드들을 회수하는 작업이 여러 차례 있었다
(`_north_ref_edges`, `_column_size` 등록 / `_core_candidates` 폐기).

`UnitRecord.geom`은 의도적으로 느슨하다 — DB Json 경계에서 null과
형태 미보장이 실제로 넘어오기 때문이다. 여기서 엄격하면 런타임에 터진다.

**부정 변증법**: silent invalid를 금지한다. 모든 도메인 명제 위반은
`CompileError`로 표면화한다. 조용히 넘어가는 게 가장 비싼 버그다.

---

## 오픈소스화 (2026-08)

- 내부 코드네임(생성기·프론트엔드·에디터 실명) 전부 역할명으로 치환
- 라이선스 Apache-2.0. 비상업 라이선스(PolyForm)를 잠시 검토했으나,
  표준 채택을 막는다는 이유로 철회
- **사업 종속 값을 저장소에서 분리**:
  - `site_filter` — 필지 선별 기준의 구조만 제공, 임계값은 설정 주입
  - `Financing` — 금융 조건 기본값 제거(`None`).
    남의 협상 조건을 조용히 물려받아 사업성을 계산하는 것이
    값이 비는 것보다 위험하다
  - 실제 값은 `site_filter.json` / `sbim_config.json` (gitignore)
- 빌드 복구: pnpm workspace 도입, `@types/three` 버전 충돌 해소
- 에디터 lockfile 레지스트리 정정(사설 미러 → npmjs)

이 시점에서 공개 이전 커밋 이력은 단일 커밋으로 정리되었다.
사업 정보가 이력에 남아 있었기 때문이다. 기술적 결정의 근거는
이 문서에 보존한다.
